{
  "name": "pose-demo",
  "private": true,
  "version": "1.0.0",
  "description": "MediaPipe pose demo static site",
  "scripts": {
    "start": "serve -s -l $PORT ."
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
} 