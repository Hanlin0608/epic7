{
  "name": "pose-demo",
  "private": true,
  "version": "1.0.0",
  "description": "MediaPipe pose demo static site",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve . -l ${PORT:-8080} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
} 